    muestra = models.PositiveIntegerField('No. de muestra')
    cilindro = models.PositiveIntegerField('No. de cilindro')
    tomada = models.CharField('Tomada de', max_length=200, null=True, blank=True)
    fecha_recibo = models.DateField('Fecha de recibo', default=datetime.date.today)
    fecha_informe = models.DateField('Fecha de informe', default=datetime.date.today)
    reporte = models.PositiveIntegerField('No. de cilindro')
    enviado = models.CharField('Enviado por', max_length=200, null=True, blank=True)
    material = models.CharField('Tipo de material', max_length=200, null=True, blank=True)
    empleo = models.CharField()